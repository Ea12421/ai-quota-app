# 引擎产出 / 个人数据(含真实用量:token、花费、限额%,勿公开)
usage.json

# 本地多 Agent 协作记录,勿公开
.agent-team/

# 依赖(打包阶段可能引入)
node_modules/

# Swift app 编译产物(.app 包,可由 app/build.sh 重新生成)
app/build/

# macOS
.DS_Store

# 日志/临时
*.log
