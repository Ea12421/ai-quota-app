# Claude Code 用量看板升级路线图

这份文档把另一窗口给出的升级建议，整理成当前项目可执行的路线图。重点不是把功能堆满，而是把看板从“记账工具”升级成“决策工具”。

## 当前状态

| 模块 | 当前能力 | 备注 |
|---|---|---|
| 菜单栏 App | 已有 macOS 菜单栏入口、小弹窗、完整窗口 | 当前已经可以本机使用 |
| 额度数据 | 已接 ClaudeMeter / Codex rate limit | 能显示 5h / 7d 等限额 |
| 本地用量 | 已读取 Claude Code / Codex 本地日志 | 不读取对话正文 |
| 模型拆分 | 已按模型统计 token / USD | 包含 input / output / cache write / cache read |
| 项目归因 | 已有项目级聚合和详情页筛选 | 下一步可做 session/window 下钻 |
| 真实成本 | 数据基础已具备 | UI 还需要把“新生成 token / 缓存读取占比”提到更显眼位置 |

## 功能路线表

| 优先级 | 功能 | 要回答的问题 | 当前状态 | 推荐做法 | 难度 |
|---|---|---|---|---|---|
| P0 | 燃尽预测 Burn-down Forecast | 按现在速度还能撑多久? | 未做 | 用最近 1h / 3h / 今日 token 速度，结合 5h / 7d 剩余额度估算剩余时间 | 中 |
| P0 | 项目归因 Project Attribution | 今天到底哪个项目在烧? | 已做项目级基础版 | 保留当前项目筛选，再补“今日 Top 项目”和后续 session 下钻 | 低-中 |
| P0 | 真实成本视图 Real Cost View | 哪些 token 是虚高，哪些是真成本? | 数据有，展示还不够突出 | 首屏增加新生成 token、缓存读取占比、USD/RMB；总 token 降为次级指标 | 低 |
| P1 | 当前窗口上下文监控 Session Context Monitor | 当前窗口快满了吗? | 未做 | 读取每个 session 最新 assistant usage，估算上下文占用率 | 中-高 |
| P1 | 窗口/session 下钻 | 同项目多个窗口分别烧了多少? | 未做 | 项目 -> session 列表，展示起止时间、token、新生成、调用数、上下文 | 中 |
| P1 | 错误/重试浪费统计 | 今天有没有被 529 / 429 / retry 浪费? | 未做 | 扫 error / apiErrorStatus / retry 相关字段，按时间段和项目汇总 | 中 |
| P2 | 模型降级省钱建议 | Opus 换 Sonnet 能省多少? | 未做 | 先做假设计算器，不自动判断任务类型 | 低-中 |
| P2 | Agent 放大倍数趋势 | 我一条消息触发多少模型调用? | 未做 | 模型调用数 / 用户消息数，按天展示趋势 | 中 |
| P2 | 任务级成本 | 这个功能/项目总共花了多少? | 未做 | 先按 session 聚合，后续支持手动打标签 | 中-高 |
| P2 | 上下文卫生提醒 | 什么时候该 clear / handoff / 开新窗口? | 未做 | 基于上下文占用率、session 时长、增长速度做提醒 | 中 |

## 建议实现顺序

| 阶段 | 目标 | 包含功能 | 验收标准 |
|---|---|---|---|
| v0.1 封版 | 当前版本稳定可提交 | 菜单栏、详情页、项目维度、基础限额、基础用量 | 能本机稳定打开，数据口径不漂，review 通过 |
| v0.2 决策层 | 从“用了多少”变成“还能撑多久” | 燃尽预测、真实成本视图优化、今日 Top 项目 | 首屏能回答剩余额度、消耗速度、主要烧钱项目 |
| v0.3 下钻层 | 找到具体窗口/会话 | session/window 下钻、当前窗口上下文监控 | 能从项目点进窗口，并看到当前上下文占用 |
| v0.4 诊断层 | 解释浪费和异常 | 错误/重试统计、上下文卫生提醒 | 能解释 overloaded / retry / 上下文过高等问题 |
| v0.5 分析层 | 任务和策略分析 | 任务级成本、模型降级假设、Agent 放大倍数 | 用于复盘长期使用方式，不影响基础看板 |

## 第一批最值得做

| 排名 | 功能 | 为什么先做 |
|---:|---|---|
| 1 | 燃尽预测 | 直接回答“我还能不能继续跑”，决策价值最高 |
| 2 | 真实成本视图 | 防止总 token 虚高吓人，把缓存读取和真正成本分开 |
| 3 | 今日 Top 项目 | 当前已经有项目数据，增量小，立刻能解释异常上涨 |
| 4 | session/window 下钻 | 多窗口使用时非常关键，但要先补 session 聚合 |
| 5 | 当前窗口上下文监控 | 价值高，但实现需要更谨慎，适合作为下一大块 |

## 数据正确性清单

| 项目 | 规则 | 当前建议 |
|---|---|---|
| 时区 | 所有按天统计必须统一时区 | 当前项目已统一用 UTC+8，后续不要混用 UTC / 本地时区 |
| cache write | 5m = input x 1.25，1h = input x 2 | 已作为核心口径保留，新增功能必须复用现有口径 |
| `<synthetic>` | 不应混入真实费用 | 新增 session / context 功能时继续过滤或单列 |
| 隐私 | 不读取、不展示 message.content | 后续所有新功能只读 usage / timestamp / cwd / sessionId / error 字段 |
| 去重 | 避免同一请求重复统计 | 新增 session/任务级功能时沿用 message.id + requestId 思路 |
| 性能 | 不要每秒全量扫 jsonl | 上下文监控应使用偏移量增量读取，必要时再加 fs.watch |

## 暂不建议做

| 功能 | 原因 |
|---|---|
| 企业级 BI 报表 | 个人重度使用场景不需要，维护成本高 |
| 复杂图表系统 | 先把指标做准，图表以后再丰富 |
| 自动任务分类 | 不读正文就很难准确，读正文又碰隐私边界 |
| 自动判断 Opus / Sonnet | 容易误导，第一版只做假设计算器 |
| 语义分析对话内容 | 和隐私原则冲突 |
| 每秒全量扫描日志 | 性能和电量都不划算 |

## 推荐下一步

先不要同时开很多线。建议下一步做 v0.2:

1. 燃尽预测。
2. 真实成本视图优化。
3. 今日 Top 项目。

这三项都基于当前已有数据，风险低、价值高，做完看板就会明显从“账单展示”变成“使用决策助手”。
